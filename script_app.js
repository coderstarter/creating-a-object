/**
 * Create a class for the mac object
 */

import mac from './mac.js';

const mac_specification = new mac("MacBook Pro", 30, "grey", 15, 26, 26, true);

console.log(mac_specification)

console.log(mac_specification.name)